import React,{Component} from 'react'
import {
    ScrollView,
    View
} from 'react-native'

class ScrollImage extends Component {
    constructor(proprs) {
        super(proprs)
    }
    render() {
        return(
            <View>
                <ScrollView>

                </ScrollView>
            </View>
        )
    }
}